# secondbrain — dependências completas (app + testes + sentence-transformers opcional na mesma lista).
# Tipicamente: pip install -r requirements.txt && pip install -e .

# --- Runtime ---
typer>=0.12.0
python-frontmatter>=1.1.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
httpx>=0.27.0
structlog>=24.1.0
chromadb>=0.5.0
pyyaml>=6.0.0
pathspec>=0.12.0
rich>=13.7.0

# --- Testes ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.21.0

# --- Embeddings locais (só necessário se EMBEDDING_PROVIDER=sentence_transformers) ---
sentence-transformers>=3.0.0
